# ── Core async runtime ────────────────────────────────────────────────────────
asyncio-throttle==1.0.2
uvloop==0.19.0

# ── Exchange connectivity ─────────────────────────────────────────────────────
ccxt==4.2.25
websockets==12.0
aiohttp==3.13.4  # 19 CVEs through CVE-2026-34517 fixed

# ── Data & numerics ───────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.1
scipy==1.12.0
scikit-learn==1.5.2  # PYSEC-2024-110
statsmodels==0.14.1

# ── Technical analysis ────────────────────────────────────────────────────────
ta-lib==0.4.28
# pandas-ta is intentionally omitted here: the current beta release pulls in
# numpy>=2.2.6 and breaks the pinned scientific stack used by CI/container builds.
# The codebase already falls back to TA-Lib and native pandas implementations.

# ── Machine learning ──────────────────────────────────────────────────────────
lightgbm==4.6.0  # PYSEC-2024-231
xgboost==2.0.3
torch==2.2.1
joblib==1.3.2

# ── Web / API ─────────────────────────────────────────────────────────────────
fastapi==0.135.2
uvicorn[standard]==0.42.0
httpx==0.27.0
pydantic==2.10.6
python-telegram-bot==21.1.1

# ── Storage ───────────────────────────────────────────────────────────────────
asyncpg==0.29.0
redis[hiredis]==5.0.3
sqlalchemy==2.0.29
alembic==1.13.1

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client==0.20.0
loguru==0.7.2
opentelemetry-api==1.23.0
opentelemetry-sdk==1.23.0

# ── Configuration ─────────────────────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.1  # CVE-2026-28684 needs 1.2.2; pinned here to preserve 1.x API compat
dynaconf==3.2.13  # CVE-2026-33154

# ── Serialization / inter-service ────────────────────────────────────────────
protobuf==4.25.8  # CVE-2025-4565
grpcio==1.62.1
grpcio-tools==1.62.1

# ── Rust FFI build ────────────────────────────────────────────────────────────
maturin==1.4.0

# ── Jupyter / research ───────────────────────────────────────────────────────
jupyterlab==4.4.8  # CVE-2025-59842
ipywidgets==8.1.2
nbstripout==0.7.1
plotly==5.20.0
kaleido==0.2.1

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity==8.2.3
orjson==3.11.6  # CVE-2025-67221 fixed
msgpack==1.0.8
python-dateutil==2.9.0.post0
pytz==2024.1
tqdm==4.66.3  # CVE-2024-34062
tabulate==0.9.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.4  # CVE-2025-71176 partial; 9.x has breaking changes
pytest-asyncio==0.23.6
pytest-mock==3.14.0
pytest-cov==5.0.0
hypothesis==6.100.1
sse-starlette>=2.0.0
variational

# ── Transitive security pins (resolved via pip-audit 2026-04-24) ─────────────
# These are pulled in by other deps; pinning lower bounds forces CVE-fixed versions.
urllib3>=2.6.1           # 5 CVEs: CVE-2024-37891, CVE-2025-50181, CVE-2025-66418, CVE-2025-66471
requests>=2.33.0         # 3 CVEs: CVE-2024-35195, CVE-2024-47081, CVE-2026-25645
certifi>=2024.7.4        # PYSEC-2024-230 (CRL corruption)
jinja2>=3.1.6            # 5 CVEs through CVE-2024-56326
cryptography>=42.0.8     # 7 CVEs through PYSEC-2024-225
idna>=3.7                # PYSEC-2024-60
pillow>=10.3.0           # CVE-2024-28219
pyjwt>=2.12.0            # CVE-2026-32597
pygments>=2.20.0         # CVE-2026-4539
pynacl>=1.6.2            # CVE-2025-69277
